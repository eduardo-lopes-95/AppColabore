import React, { useState } from 'react';
import { Center, Box, Heading, VStack, Input, Button, Checkbox, Select, Pressable, Text } from 'native-base';
import { MaterialIcons } from '@expo/vector-icons';

const Logo = () => {

  return (
    <Center flex={1}>
      <Text fontSize="2xl" fontWeight="bold" color="white">
          Colabore
      </Text>
    </Center>
  );
};

export default Logo;
